# 스크랩퍼

지정된 웹 페이지의 게시글 목록을 스크래핑하여 정형화된 JSON 데이터로 반환하는 모듈입니다.

## 🛠 기술 스택

- Python 3.12.7
- requests
- BeautifulSoup4
- pytest

---

## 프로젝트 구조 예시

```
scraper/
│
├── README.md
├── tests/
└── scraper/
    ├── mock/
    ├── main.py
    ├── article_parser.py
    └── url_utils.py
```

---

## 데이터 구조 (DTO)

스크래핑 결과는 아래 구조로 반환됩니다.

```json
{
  "id": "string",
  "url": "string",
  "title": "string",
  "thumbnail": "string",
  "category": {
    "name": "string",
    "url": "string"
  },
  "excerpt": "string",
  "author": {
    "name": "string",
    "url": "string"
  },
  "published_date": "string"
}
```

### 필드 설명

| 필드           | 타입 | 설명                   |
| -------------- | ---- | ---------------------- |
| id             | str  | 게시글 고유 ID         |
| url            | str  | 게시글 상세 페이지 URL |
| title          | str  | 게시글 제목            |
| thumbnail      | str  | 썸네일 이미지 URL      |
| category.name  | str  | 카테고리 이름          |
| category.url   | str  | 카테고리 URL           |
| excerpt        | str  | 게시글 요약            |
| author.name    | str  | 작성자 이름            |
| author.url     | str  | 작성자 페이지 URL      |
| published_date | str  | 게시 날짜              |

---

## 실행 방법

---

## 동작 흐름

1. 카테고리 페이지 요청
2. 게시글 목록 HTML 파싱
3. 각 게시글 데이터 추출
   - 데이터 값 존재 확인
   - 데이터 값 유형 확인
4. JSON 반환

---

## 예외 처리 전략

- HTTP 요청 실패 시 예외 발생
- None 값은 빈 문자열로 변환
- 1차 검증: 필수 필드 누락 시 예외 발생
- 2차 검증: 날짜, URL 형식 검증 후 반환
